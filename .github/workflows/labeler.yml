name: Label PRs

on:
  pull_request_target:

permissions:
  contents: read
  pull-requests: write

jobs:
  labeler:
    name: Apply area labels
    runs-on: ubuntu-latest
    steps:
      - name: Label based on changed paths
        uses: actions/labeler@v6
        with:
          sync-labels: true
